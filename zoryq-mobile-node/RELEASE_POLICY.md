# Release policy

The rolling testnet APK release is explicitly beta software. Production/mainnet releases must use a stable project-controlled signing key stored only as an encrypted CI secret, never committed to the repository.
