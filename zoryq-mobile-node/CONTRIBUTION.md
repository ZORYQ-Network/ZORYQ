# Contribution semantics

A mobile installation contributes independent network observation only when it validates ZORYQ chain identity and block progression itself. It should not be counted as an independent network participant merely because the app is installed.
