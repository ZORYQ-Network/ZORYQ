(()=>{
'use strict';
const KEY='zoriq.social.suite.v1';
const seed={profile:{name:'Explorer',handle:'@explorer',bio:'Construindo, explorando e aprendendo na ZORIQ.',location:'',website:'',discoverable:true,showReputation:true,dmMode:'following',private:false},following:['lena','maya'],blocked:[],muted:[],bookmarks:[],comments:{},notifications:[{id:'n1',icon:'✦',title:'Nova conquista',text:'Você desbloqueou o primeiro nível do perfil evolutivo.',time:'agora',read:false},{id:'n2',icon:'🤝',title:'Maya seguiu você',text:'Vocês têm interesses em comum em Builders e Technology.',time:'12 min',read:false},{id:'n3',icon:'🌎',title:'Crypto World',text:'Uma nova conversa Pulse começou no World.',time:'1 h',read:true}],threads:{lena:{name:'Lena Ortiz',handle:'@lena',unread:1,messages:[{me:false,text:'Vi seu post sobre feeds controlados pelo usuário. Boa ideia.',at:'09:42'}]},maya:{name:'Maya Chen',handle:'@maya.builds',unread:0,messages:[{me:false,text:'Vamos colaborar em uma ideia para Builders?',at:'ontem'}]}},achievements:{firstPost:true,firstWorld:true,firstCollab:true,streak7:true,tenFollows:false,rep90:false},drafts:[],settings:{hideCounts:false,autoplay:false,reduceMotion:false,allowMentions:true,allowTagging:true,qualityFilter:true}};
const people=[
{id:'lena',name:'Lena Ortiz',handle:'@lena',bio:'Product designer · feeds transparentes · comunidades.',rep:96,tags:['Design','Tech','Creators']},
{id:'maya',name:'Maya Chen',handle:'@maya.builds',bio:'Builder, open source e colaboração global.',rep:94,tags:['Builders','AI','Open Source']},
{id:'kai',name:'Kai',handle:'@kai.play',bio:'Indie games, protótipos e desafios públicos.',rep:88,tags:['Gaming','Creators']},
{id:'nora',name:'Nora Silva',handle:'@nora.crypto',bio:'Segurança Web3, carteiras e educação.',rep:97,tags:['Crypto','Security','Learn']},
{id:'alex',name:'Alex Rivera',handle:'@alex.music',bio:'Música ao vivo, comunidades e experiências sociais.',rep:91,tags:['Music','Live']},
{id:'sam',name:'Sam Lee',handle:'@sam.ai',bio:'Agentes de IA, software e futuro do trabalho.',rep:93,tags:['AI','Tech','Builders']}
];
const topics=[['AI Agents','Technology · Builders',97],['Human First','Trust & Safety',94],['Wallet Security','Crypto · Learn',92],['Indie Games','Gaming',88],['Creator Economy','Creators',86],['Open Source','Technology',84]];
let state=load();
const $=(q,r=document)=>r.querySelector(q),$$=(q,r=document)=>[...r.querySelectorAll(q)];
function clone(x){return JSON.parse(JSON.stringify(x))}
function merge(a,b){if(Array.isArray(a))return Array.isArray(b)?b:a;if(!a||typeof a!=='object')return b??a;const out={...a};for(const k of Object.keys(b||{}))out[k]=a[k]&&typeof a[k]==='object'&&!Array.isArray(a[k])&&typeof b[k]==='object'&&!Array.isArray(b[k])?merge(a[k],b[k]):b[k];return out}
function load(){try{return merge(clone(seed),JSON.parse(localStorage.getItem(KEY)||'{}'))}catch{return clone(seed)}}
function save(){localStorage.setItem(KEY,JSON.stringify(state));updateBadges();syncProfile()}
function esc(s=''){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function uid(){return Math.random().toString(36).slice(2)+Date.now().toString(36)}
function toast(msg){const t=$('#toast');if(!t)return;t.textContent=msg;t.classList.add('show');clearTimeout(toast._);toast._=setTimeout(()=>t.classList.remove('show'),1800)}
function showSuiteView(id){$$('.view').forEach(v=>v.classList.toggle('active',v.id===`view-${id}`));$$('[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===id));window.scrollTo({top:0,behavior:'smooth'})}
function addNav(label,id){const nav=$('.nav');if(!nav||$(`[data-view="${id}"]`,nav))return;const b=document.createElement('button');b.className='nav-btn';b.dataset.view=id;b.innerHTML=`<span class="nav-dot"></span><span class="label">${label}</span>`;b.onclick=()=>showSuiteView(id);nav.appendChild(b)}
function appendView(id,html){if($(`#view-${id}`))return;const v=document.createElement('section');v.className='view';v.id=`view-${id}`;v.innerHTML=html;$('.center').appendChild(v)}
function syncProfile(){$$('[data-profile-name]').forEach(x=>x.textContent=state.profile.name);const h=state.profile.handle;const p=$('#suiteProfileHandle');if(p)p.textContent=h}
function unreadCount(){return state.notifications.filter(n=>!n.read).length}
function dmUnread(){return Object.values(state.threads).reduce((a,t)=>a+Number(t.unread||0),0)}
function updateBadges(){const a=$('#suiteAlertBadge'),d=$('#suiteDmBadge');if(a){const n=unreadCount();a.textContent=n;a.style.display=n?'grid':'none'}if(d){const n=dmUnread();d.textContent=n;d.style.display=n?'grid':'none'}}
function buildHeaderTools(){const actions=$('.top-actions');if(!actions||$('#suiteSearchBtn'))return;const wrap=document.createElement('div');wrap.className='suite-toolbar';wrap.innerHTML=`<button class="suite-icon" id="suiteSearchBtn" title="Buscar">⌕</button><button class="suite-icon" id="suiteDmBtn" title="Mensagens">✉<span class="suite-badge" id="suiteDmBadge"></span></button><button class="suite-icon" id="suiteAlertBtn" title="Notificações">◉<span class="suite-badge" id="suiteAlertBadge"></span></button>`;actions.prepend(wrap);$('#suiteSearchBtn').onclick=()=>showSuiteView('discover');$('#suiteDmBtn').onclick=()=>showSuiteView('inbox');$('#suiteAlertBtn').onclick=()=>showSuiteView('alerts')}
function renderDiscover(){const root=$('#suiteDiscover');if(!root)return;const q=($('#suiteSearchInput')?.value||'').trim().toLowerCase();const filtered=people.filter(p=>!state.blocked.includes(p.id)&&(!q||[p.name,p.handle,p.bio,...p.tags].join(' ').toLowerCase().includes(q)));root.innerHTML=`<div class="suite-grid">${filtered.map(userCard).join('')}</div>${!filtered.length?'<div class="suite-empty">Nenhuma pessoa encontrada.</div>':''}`;wirePeople()}
function userCard(p){const following=state.following.includes(p.id);return `<div class="suite-user" data-person="${p.id}"><div class="suite-user-head"><div class="suite-user-avatar">${esc(p.name.slice(0,2).toUpperCase())}</div><div class="suite-user-meta"><div class="suite-user-name">${esc(p.name)}</div><div class="suite-user-handle">${esc(p.handle)} · rep ${p.rep}</div></div></div><div class="suite-user-bio">${esc(p.bio)}</div><div class="suite-chip-row">${p.tags.map(t=>`<span class="suite-chip">${esc(t)}</span>`).join('')}</div><div class="row between"><button class="suite-follow ${following?'following':''}" data-follow="${p.id}">${following?'Seguindo ✓':'Seguir'}</button><button class="secondary" data-open-profile="${p.id}">Ver perfil</button></div></div>`}
function wirePeople(){$$('[data-follow]').forEach(b=>b.onclick=()=>toggleFollow(b.dataset.follow));$$('[data-open-profile]').forEach(b=>b.onclick=()=>openPerson(b.dataset.openProfile))}
function toggleFollow(id){const i=state.following.indexOf(id);if(i>=0){state.following.splice(i,1);toast('Você deixou de seguir')}else{state.following.push(id);pushNotification('🤝','Novo vínculo',`Agora você segue ${people.find(p=>p.id===id)?.name||'esta pessoa'}.`);toast('Seguindo')};state.achievements.tenFollows=state.following.length>=10;save();renderDiscover();renderSuggested()}
function openPerson(id){const p=people.find(x=>x.id===id);if(!p)return;const following=state.following.includes(id),blocked=state.blocked.includes(id);const modal=$('#suiteModal');modal.querySelector('.suite-modal-card').innerHTML=`<div class="suite-modal-head"><b>Perfil</b><button class="suite-close" data-close>×</button></div><div class="suite-profile-card"><div class="suite-user-head"><div class="suite-user-avatar">${esc(p.name.slice(0,2).toUpperCase())}</div><div><h2 style="margin:0">${esc(p.name)}</h2><div class="suite-user-handle">${esc(p.handle)} · reputação ${p.rep}/100</div></div></div><p class="suite-profile-bio">${esc(p.bio)}</p><div class="suite-profile-tags">${p.tags.map(t=>`<span class="suite-chip">${esc(t)}</span>`).join('')}</div><div class="suite-profile-actions"><button class="suite-follow ${following?'following':''}" id="modalFollow">${following?'Seguindo ✓':'Seguir'}</button><button class="secondary" id="modalMessage">Mensagem</button><button class="secondary suite-danger" id="modalBlock">${blocked?'Desbloquear':'Bloquear'}</button></div></div>`;modal.classList.add('open');$('[data-close]',modal).onclick=()=>modal.classList.remove('open');$('#modalFollow').onclick=()=>{toggleFollow(id);openPerson(id)};$('#modalMessage').onclick=()=>{ensureThread(id);modal.classList.remove('open');openThread(id)};$('#modalBlock').onclick=()=>{toggleBlock(id);modal.classList.remove('open');renderDiscover()}}
function toggleBlock(id){const i=state.blocked.indexOf(id);if(i>=0)state.blocked.splice(i,1);else{state.blocked.push(id);state.following=state.following.filter(x=>x!==id)}save();toast(state.blocked.includes(id)?'Perfil bloqueado':'Perfil desbloqueado')}
function ensureThread(id){if(!state.threads[id]){const p=people.find(x=>x.id===id);state.threads[id]={name:p?.name||id,handle:p?.handle||'@user',unread:0,messages:[]};save()}}
function renderInbox(){const root=$('#suiteInbox');if(!root)return;const entries=Object.entries(state.threads);root.innerHTML=entries.map(([id,t])=>`<div class="suite-thread" data-thread="${id}"><div class="suite-user-avatar">${esc(t.name.slice(0,2).toUpperCase())}</div><div class="suite-thread-body"><div class="row between"><b>${esc(t.name)}</b><span class="suite-thread-time">${esc(t.messages.at(-1)?.at||'')}</span></div><div class="suite-thread-preview">${esc(t.messages.at(-1)?.text||'Inicie uma conversa')}</div></div>${t.unread?'<span class="suite-unread"></span>':''}</div>`).join('')||'<div class="suite-empty">Nenhuma conversa ainda.</div>';$$('[data-thread]',root).forEach(x=>x.onclick=()=>openThread(x.dataset.thread))}
function openThread(id){ensureThread(id);const t=state.threads[id];t.unread=0;save();const root=$('#suiteInbox');root.innerHTML=`<div class="suite-chat"><div class="suite-chat-head"><button class="secondary" id="threadBack">←</button><div class="suite-user-avatar">${esc(t.name.slice(0,2).toUpperCase())}</div><div><b>${esc(t.name)}</b><div class="suite-user-handle">${esc(t.handle)}</div></div></div><div class="suite-chat-log" id="suiteChatLog">${t.messages.map(m=>`<div class="suite-message ${m.me?'me':''}">${esc(m.text)}<div class="suite-thread-time" style="margin-top:4px">${esc(m.at)}</div></div>`).join('')}</div><div class="suite-chat-form"><input id="suiteMessageInput" maxlength="500" placeholder="Mensagem..."><button class="primary" id="suiteSendMessage">Enviar</button></div></div>`;$('#threadBack').onclick=renderInbox;$('#suiteSendMessage').onclick=()=>sendMessage(id);$('#suiteMessageInput').addEventListener('keydown',e=>{if(e.key==='Enter')sendMessage(id)});setTimeout(()=>{const l=$('#suiteChatLog');if(l)l.scrollTop=l.scrollHeight},0)}
function sendMessage(id){const input=$('#suiteMessageInput'),text=input.value.trim();if(!text)return;state.threads[id].messages.push({me:true,text,at:new Date().toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})});save();openThread(id)}
function pushNotification(icon,title,text){state.notifications.unshift({id:uid(),icon,title,text,time:'agora',read:false});state.notifications=state.notifications.slice(0,40)}
function renderAlerts(){const root=$('#suiteAlerts');if(!root)return;root.innerHTML=`<div class="row between" style="margin-bottom:10px"><span class="muted">${unreadCount()} não lidas</span><button class="secondary" id="suiteReadAll">Marcar todas como lidas</button></div>${state.notifications.map(n=>`<div class="suite-alert ${n.read?'':'unread'}" data-alert="${n.id}"><div class="suite-alert-icon">${esc(n.icon)}</div><div class="suite-alert-body"><b>${esc(n.title)}</b><p>${esc(n.text)}</p><span class="suite-alert-time">${esc(n.time)}</span></div></div>`).join('')||'<div class="suite-empty">Sem notificações.</div>'}`;$('#suiteReadAll').onclick=()=>{state.notifications.forEach(n=>n.read=true);save();renderAlerts()};$$('[data-alert]').forEach(a=>a.onclick=()=>{const n=state.notifications.find(x=>x.id===a.dataset.alert);if(n)n.read=true;save();renderAlerts()})}
function renderBookmarks(){const root=$('#suiteBookmarks');if(!root)return;root.innerHTML=state.bookmarks.map(b=>`<div class="suite-bookmark"><div class="row between"><span class="tag">SALVO</span><button class="suite-post-tool" data-remove-bookmark="${b.id}">Remover</button></div><p>${esc(b.text)}</p><span class="suite-counter">${esc(b.author||'ZORIQ')}</span></div>`).join('')||'<div class="suite-empty">Posts que você salvar aparecerão aqui.</div>';$$('[data-remove-bookmark]').forEach(b=>b.onclick=()=>{state.bookmarks=state.bookmarks.filter(x=>x.id!==b.dataset.removeBookmark);save();renderBookmarks();decoratePosts()})}
function renderSettings(){const root=$('#suiteSettings');if(!root)return;const items=[['private','Conta privada','Aprovar novos seguidores antes que vejam seu conteúdo.',state.profile.private,'profile'],['discoverable','Aparecer em descoberta','Permite que outras pessoas encontrem seu perfil na busca.',state.profile.discoverable,'profile'],['showReputation','Mostrar reputação','Exibe seu Reputation Graph no perfil.',state.profile.showReputation,'profile'],['qualityFilter','Filtro de qualidade','Reduz spam e conteúdo repetitivo na experiência local.',state.settings.qualityFilter,'settings'],['allowMentions','Permitir menções','Outras pessoas podem mencionar seu @handle.',state.settings.allowMentions,'settings'],['allowTagging','Permitir marcações','Outras pessoas podem marcar seu perfil.',state.settings.allowTagging,'settings'],['hideCounts','Ocultar contagens','Esconde números de engajamento no seu próprio cliente.',state.settings.hideCounts,'settings'],['reduceMotion','Reduzir movimento','Prefere menos animações e efeitos visuais.',state.settings.reduceMotion,'settings']];root.innerHTML=`<div class="suite-settings">${items.map(([id,title,desc,on,scope])=>`<div class="suite-setting"><div class="suite-setting-copy"><b>${title}</b><span>${desc}</span></div><button class="suite-switch ${on?'on':''}" data-setting="${id}" data-scope="${scope}"><i></i></button></div>`).join('')}</div>`;$$('[data-setting]').forEach(b=>b.onclick=()=>{const scope=b.dataset.scope,id=b.dataset.setting;state[scope][id]=!state[scope][id];save();renderSettings();applyPreferences()})}
function applyPreferences(){document.documentElement.style.scrollBehavior=state.settings.reduceMotion?'auto':'';$$('.actions').forEach(a=>a.style.display=state.settings.hideCounts?'none':'')}
function renderEditor(){const root=$('#suiteProfileEditor');if(!root)return;root.innerHTML=`<div class="suite-editor"><label>Nome<input id="editName" maxlength="40" value="${esc(state.profile.name)}"></label><label>@handle<input id="editHandle" maxlength="30" value="${esc(state.profile.handle)}"></label><label>Bio<textarea id="editBio" maxlength="180">${esc(state.profile.bio)}</textarea></label><label>Localização<input id="editLocation" maxlength="60" value="${esc(state.profile.location)}"></label><label>Website<input id="editWebsite" maxlength="120" value="${esc(state.profile.website)}"></label><button class="primary" id="saveProfile">Salvar perfil</button></div>`;$('#saveProfile').onclick=()=>{const name=$('#editName').value.trim(),handle=$('#editHandle').value.trim(),bio=$('#editBio').value.trim();if(!name||!/^@[a-zA-Z0-9_.]{3,29}$/.test(handle))return toast('Confira nome e @handle');state.profile={...state.profile,name,handle,bio,location:$('#editLocation').value.trim(),website:$('#editWebsite').value.trim()};save();renderEditor();toast('Perfil atualizado')}
function renderAchievements(){const root=$('#suiteAchievements');if(!root)return;const defs=[['firstPost','✍','Primeiro post','Publique seu primeiro conteúdo.'],['firstWorld','🌎','Primeiro World','Participe de uma comunidade.'],['firstCollab','🤝','Colaborador','Participe de um post colaborativo.'],['streak7','🔥','Sequência 7 dias','Mantenha uma sequência de sete dias.'],['tenFollows','◉','Conector','Siga 10 pessoas.'],['rep90','◇','Reputação 90+','Alcance reputação 90 ou mais.']];root.innerHTML=defs.map(([id,icon,title,desc])=>`<div class="suite-achievement ${state.achievements[id]?'':'locked'}"><div class="suite-achievement-icon">${icon}</div><div><b>${title}</b><small>${desc}</small></div><span class="tag">${state.achievements[id]?'CONQUISTADO':'BLOQUEADO'}</span></div>`).join('')}
function renderSuggested(){const box=$('#suiteSuggested');if(!box)return;const picks=people.filter(p=>!state.following.includes(p.id)&&!state.blocked.includes(p.id)).slice(0,3);box.innerHTML=picks.map(p=>`<div class="world-mini"><span>${esc(p.name)}</span><button class="suite-follow" data-follow="${p.id}">Seguir</button></div>`).join('');wirePeople()}
function decoratePosts(){const posts=$$('.post');posts.forEach((post,i)=>{if(post.dataset.suiteDecorated)return;post.dataset.suiteDecorated='1';const text=$('.post-body',post)?.textContent?.trim()||'',author=$('.post-user',post)?.textContent?.trim()||'ZORIQ',id='post-'+hash(text+author);const tools=document.createElement('div');tools.className='suite-post-tools';const saved=state.bookmarks.some(x=>x.id===id);tools.innerHTML=`<button class="suite-post-tool ${saved?'active':''}" data-suite-save>☆ ${saved?'Salvo':'Salvar'}</button><button class="suite-post-tool" data-suite-comment>💬 Comentar</button><button class="suite-post-tool" data-suite-more>•••</button>`;post.appendChild(tools);const comments=document.createElement('div');comments.className='suite-comments';comments.style.display='none';comments.innerHTML=commentsHtml(id);post.appendChild(comments);$('[data-suite-save]',tools).onclick=()=>{const idx=state.bookmarks.findIndex(x=>x.id===id);if(idx>=0)state.bookmarks.splice(idx,1);else state.bookmarks.unshift({id,text,author});save();decorateRefresh(post,id,text,author);toast(idx>=0?'Removido dos salvos':'Post salvo')};$('[data-suite-comment]',tools).onclick=()=>{comments.style.display=comments.style.display==='none'?'block':'none'};$('[data-suite-more]',tools).onclick=()=>openPostMenu(author);wireCommentForm(post,id)})}
function decorateRefresh(post,id,text,author){post.dataset.suiteDecorated='';$('.suite-post-tools',post)?.remove();$('.suite-comments',post)?.remove();decoratePosts()}
function commentsHtml(id){const rows=state.comments[id]||[];return `<div>${rows.map(c=>`<div class="suite-comment"><div class="suite-comment-avatar">YOU</div><div class="suite-comment-body"><b>${esc(c.name)}</b>${esc(c.text)}</div></div>`).join('')}</div><div class="suite-comment-form"><input maxlength="220" placeholder="Escreva um comentário..."><button class="primary" data-comment-send>Enviar</button></div>`}
function wireCommentForm(post,id){const form=$('.suite-comment-form',post);if(!form)return;const input=$('input',form),send=$('[data-comment-send]',form);const go=()=>{const text=input.value.trim();if(!text)return;state.comments[id]=state.comments[id]||[];state.comments[id].push({name:state.profile.name,text,at:Date.now()});pushNotification('💬','Comentário publicado','Seu comentário foi adicionado ao post nesta demonstração.');save();const holder=$('.suite-comments',post);holder.innerHTML=commentsHtml(id);holder.style.display='block';wireCommentForm(post,id)};send.onclick=go;input.addEventListener('keydown',e=>{if(e.key==='Enter')go()})}
function openPostMenu(author){const modal=$('#suiteModal');modal.querySelector('.suite-modal-card').innerHTML=`<div class="suite-modal-head"><b>Opções do post</b><button class="suite-close" data-close>×</button></div><div class="suite-settings"><button class="secondary">Não tenho interesse neste conteúdo</button><button class="secondary">Silenciar ${esc(author)}</button><button class="secondary suite-danger">Denunciar conteúdo</button></div><p class="notice" style="margin-top:12px">Nesta versão essas ações afetam apenas a experiência local. Moderação real exige backend, política e fluxo de recurso.</p>`;modal.classList.add('open');$('[data-close]',modal).onclick=()=>modal.classList.remove('open')}
function hash(s){let h=2166136261;for(let i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619)}return (h>>>0).toString(36)}
function installObserver(){const feed=$('#feed');if(!feed)return;const o=new MutationObserver(()=>{decoratePosts();applyPreferences();state.achievements.firstPost=Boolean(document.querySelector('.post .tag')?.textContent.includes('SEU POST'))||state.achievements.firstPost;save()});o.observe(feed,{childList:true,subtree:true});decoratePosts()}
function buildViews(){addNav('Descobrir','discover');addNav('Mensagens','inbox');addNav('Alertas','alerts');addNav('Salvos','bookmarks');addNav('Privacidade','privacy');appendView('discover',`<div class="hero"><div class="eyebrow">DISCOVERY</div><h1>Encontre pessoas e assuntos <span class="gradient">relevantes.</span></h1><p>Busca local por pessoas, interesses e reputação. Em produção, ranking precisa de diversidade, segurança e controles do usuário.</p></div><div class="suite-search"><input id="suiteSearchInput" placeholder="Buscar pessoas, @handles e interesses"><button class="primary" id="suiteSearchGo">Buscar</button></div><div class="suite-tabs"><button class="suite-tab active">Pessoas</button><button class="suite-tab">Assuntos</button><button class="suite-tab">Worlds</button></div><div id="suiteDiscover"></div><section class="card"><h3>Assuntos em alta para você</h3><div id="suiteTopics"></div></section>`);appendView('inbox',`<div class="hero"><div class="eyebrow">ZORIQ MESSAGES</div><h1>Conversas privadas, <span class="gradient">simples.</span></h1><p>Mensagens desta versão ficam neste navegador. Criptografia ponta a ponta e sincronização multi-dispositivo exigem protocolo próprio antes de produção.</p></div><div id="suiteInbox" style="margin-top:14px"></div>`);appendView('alerts',`<div class="hero"><div class="eyebrow">NOTIFICAÇÕES</div><h1>Veja o que merece <span class="gradient">sua atenção.</span></h1><p>Conquistas, vínculos, comentários, Worlds e atividades relevantes em uma central única.</p></div><div id="suiteAlerts" style="margin-top:14px"></div>`);appendView('bookmarks',`<div class="hero"><div class="eyebrow">SALVOS</div><h1>Sua biblioteca <span class="gradient">social.</span></h1><p>Guarde posts e ideias para voltar depois.</p></div><div id="suiteBookmarks" style="margin-top:14px"></div>`);appendView('privacy',`<div class="hero"><div class="eyebrow">PRIVACIDADE & CONTROLE</div><h1>A rede deve se adaptar <span class="gradient">a você.</span></h1><p>Controle descoberta, reputação, menções, filtros e preferências de experiência.</p></div><section class="card" style="margin-top:14px"><h2>Perfil</h2><div id="suiteProfileEditor"></div></section><section class="card"><h2>Privacidade e experiência</h2><div id="suiteSettings"></div></section><section class="card"><h2>Conquistas</h2><div class="suite-settings" id="suiteAchievements"></div></section>`);const rail=$('.rail');if(rail&&!$('#suiteSuggested')){const s=document.createElement('section');s.className='card';s.innerHTML='<h3>Pessoas para conhecer</h3><div id="suiteSuggested"></div><button class="secondary" data-suite-discover style="width:100%;margin-top:10px">Abrir descoberta</button>';rail.insertBefore(s,rail.children[1]||null);$('[data-suite-discover]',s).onclick=()=>showSuiteView('discover')}}
function wireViews(){const input=$('#suiteSearchInput');if(input){input.oninput=renderDiscover;$('#suiteSearchGo').onclick=renderDiscover}renderDiscover();renderInbox();renderAlerts();renderBookmarks();renderSettings();renderEditor();renderAchievements();renderSuggested();const topic=$('#suiteTopics');if(topic)topic.innerHTML=topics.map(([n,d,score])=>`<div class="suite-topic"><div><b>${esc(n)}</b><div class="suite-user-handle">${esc(d)}</div></div><span class="suite-score">match ${score}%</span></div>`).join('')}
function addModal(){if($('#suiteModal'))return;const m=document.createElement('div');m.className='suite-modal';m.id='suiteModal';m.innerHTML='<div class="suite-modal-card"></div>';m.addEventListener('click',e=>{if(e.target===m)m.classList.remove('open')});document.body.appendChild(m)}
function init(){buildHeaderTools();buildViews();addModal();wireViews();installObserver();syncProfile();updateBadges();applyPreferences();state.achievements.rep90=Number(document.querySelector('[data-reputation]')?.textContent||0)>=90;save()}
document.addEventListener('DOMContentLoaded',init);
})();