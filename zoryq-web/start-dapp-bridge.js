(()=>{const $=id=>document.getElementById(id);function install(){const plan=$('plan');const out=$('planOut');const directive=$('directive');if(!plan||!out||!directive||$('buildDapp'))return;const wrap=document.createElement('div');wrap.className='row';wrap.style.marginTop='10px';const a=document.createElement('a');a.id='buildDapp';a.className='btn';a.href='/dapp-builder.html';a.textContent='✨ Transformar em dApp';a.title='Levar este objetivo para o ZORYQ AI dApp Builder';wrap.appendChild(a);const hint=document.createElement('span');hint.className='muted';hint.style.alignSelf='center';hint.style.fontSize='12px';hint.textContent='Preview → carteira → deploy on-chain';wrap.appendChild(hint);out.insertAdjacentElement('afterend',wrap);function sync(){const text=directive.value.trim();a.href='/dapp-builder.html'+(text?'?prompt='+encodeURIComponent(text):'');}directive.addEventListener('input',sync);sync();const observer=new MutationObserver(()=>{if(!out.hidden){a.classList.add('primary');a.textContent='🚀 Criar este dApp e fazer deploy';sync();}});observer.observe(out,{attributes:true,attributeFilter:['hidden']});}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();})();