# ZORYQ EVM Testnet deployment trigger

Railway production service: `zoryq-evm-node`.

This file exists to trigger a fresh deployment after the service root directory and public network variables were configured.
