# ZORYQ EVM Testnet deployment trigger

Railway production candidate service: `zoryq-evm-node-v5`.

This file intentionally participates in the Railway watch pattern `zoryq-evm-node/**` so infrastructure-only commits can trigger a fresh public Testnet deployment after Docker, healthcheck, domain and network variables are configured.

Trigger generation: v5-bootstrap-2026-09-05
