name: Validate One-Prompt Company Demo

on:
  workflow_dispatch:
  pull_request:
    branches: [zoryq-evm-testnet-node]
    paths:
      - 'zoryq-web/proof.html'
      - 'zoryq-contracts/src/ZoryqOnePromptCompany.sol'
      - '.github/workflows/zoryq-autonomous-economy-ui.yml'
  push:
    branches: [zoryq-evm-testnet-node]
    paths:
      - 'zoryq-web/proof.html'
      - 'zoryq-contracts/src/ZoryqOnePromptCompany.sol'
      - '.github/workflows/zoryq-autonomous-economy-ui.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v6
      - name: Extract and syntax-check inline JavaScript
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re
          html = Path('zoryq-web/proof.html').read_text(encoding='utf-8')
          blocks = re.findall(r'<script>(.*?)</script>', html, flags=re.S|re.I)
          if not blocks:
              raise SystemExit('proof.html contains no inline script')
          Path('/tmp/zoryq-proof.js').write_text('\n'.join(blocks), encoding='utf-8')
          PY
          node --check /tmp/zoryq-proof.js
      - name: Assert one-prompt UX and testnet boundaries
        run: |
          grep -q 'Crie uma empresa digital com US$100.' zoryq-web/proof.html
          grep -q 'CRIAR EMPRESA ON-CHAIN' zoryq-web/proof.html
          grep -q 'dUSD' zoryq-web/proof.html
          grep -qi 'não é dólar real' zoryq-web/proof.html
          ! grep -Eqi 'private[ _-]?key|seed phrase|mnemonic' zoryq-web/proof.html
      - name: Verify public UI and canonical one-prompt transaction
        env:
          BASE: https://zoryq-evm-node-live-production.up.railway.app
          CONTRACT: '0xF450eF5D7b9196ebd878a6Fa703b855a9F13E74E'
          DEMO_USD: '0xc8785dfdA1D2C55d775D27e8e6A1D68953ab6f8c'
          DEPLOY_TX: '0x15e35ec56b0dadc03a72c9aeb88fa3f2fb52345b19dd40833037859a804b612c'
          LAUNCH_TX: '0xd0c5bf3b8e73d23b2bdb6e4f5320b98d65c5393d37bce6cf12ca051a7d87f790'
        run: |
          python3 - <<'PY'
          import json, os, time, urllib.request
          base=os.environ['BASE'].rstrip('/')
          rpc_url=base+'/rpc'
          def get(url):
              last=None
              for _ in range(12):
                  try:
                      with urllib.request.urlopen(url, timeout=15) as r:
                          return r.read().decode('utf-8')
                  except Exception as e:
                      last=e; time.sleep(3)
              raise last
          def rpc(method, params=None):
              data=json.dumps({'jsonrpc':'2.0','id':1,'method':method,'params':params or []}).encode()
              req=urllib.request.Request(rpc_url,data=data,headers={'content-type':'application/json'})
              with urllib.request.urlopen(req, timeout=20) as r:
                  out=json.loads(r.read())
              if out.get('error'):
                  raise RuntimeError(out['error'])
              return out.get('result')
          html=get(base+'/proof')
          assert 'Crie uma empresa digital com US$100.' in html, 'one-prompt UI not served yet'
          assert 'CRIAR EMPRESA ON-CHAIN' in html, 'launch action missing'
          assert rpc('eth_chainId')=='0x5a5159', 'wrong chain id'
          for address in (os.environ['CONTRACT'],os.environ['DEMO_USD']):
              code=rpc('eth_getCode',[address,'latest'])
              assert isinstance(code,str) and code!='0x' and len(code)>10, f'bytecode missing at {address}'
          deploy=rpc('eth_getTransactionReceipt',[os.environ['DEPLOY_TX']])
          launch=rpc('eth_getTransactionReceipt',[os.environ['LAUNCH_TX']])
          assert deploy and deploy.get('status')=='0x1', 'company contract deployment failed'
          assert launch and launch.get('status')=='0x1', 'canonical company launch failed'
          assert len(launch.get('logs') or []) >= 20, 'canonical launch did not emit the expected rich lifecycle evidence'
          print('One-Prompt Company verified: UI + contract + dUSD + canonical launch + lifecycle event trail')
          PY
