name: ZORYQ Factory Per-App APK

on:
  workflow_dispatch:
    inputs:
      app_name:
        description: Display name for the generated Android app
        required: true
        default: ZORYQ Generated App
      app_slug:
        description: Lowercase app slug used in the APK filename
        required: true
        default: generated-app
      app_url:
        description: Full ZORYQ generated app URL
        required: true
  pull_request:
    paths:
      - '.github/workflows/factory-app-apk.yml'
      - 'apps/budget-dapp-android/**'
      - 'zoryq-web/project-launch.html'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      APP_NAME: ${{ github.event_name == 'workflow_dispatch' && inputs.app_name || 'ZORYQ Factory Smoke App' }}
      APP_SLUG: ${{ github.event_name == 'workflow_dispatch' && inputs.app_slug || 'factory-smoke-app' }}
      APP_URL: ${{ github.event_name == 'workflow_dispatch' && inputs.app_url || 'https://zoryq-evm-node-live-production.up.railway.app/project-launch#app=demo' }}
    steps:
      - uses: actions/checkout@v4

      - name: Validate inputs
        shell: bash
        run: |
          set -euo pipefail
          [[ "$APP_URL" == https://zoryq-evm-node-live-production.up.railway.app/project-launch* ]]
          [[ "$APP_SLUG" =~ ^[a-z0-9][a-z0-9-]{0,39}$ ]]
          test -n "$APP_NAME"

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Personalize Android wrapper
        shell: python
        run: |
          from pathlib import Path
          import os,re
          root=Path('apps/budget-dapp-android')
          java=root/'app/src/main/java/network/zoryq/budgetforecast/MainActivity.java'
          manifest=root/'app/src/main/AndroidManifest.xml'
          gradle=root/'app/build.gradle'
          src=java.read_text()
          url=os.environ['APP_URL'].replace('\\','\\\\').replace('"','\\"')
          src=re.sub(r'private static final String FACTORY_URL = .*?;', f'private static final String FACTORY_URL = "{url}";', src)
          src=src.replace(' ZORYQ-App-Runner-Android/1.1',' ZORYQ-Per-App-Android/0.2')
          java.write_text(src)
          name=os.environ['APP_NAME'].replace('&','&amp;').replace('"','&quot;')[:60]
          m=manifest.read_text()
          m=re.sub(r'android:label="[^"]*"', f'android:label="{name}"', m)
          manifest.write_text(m)
          suffix=re.sub(r'[^a-z0-9]','',os.environ['APP_SLUG'].lower())[:24] or 'generatedapp'
          g=gradle.read_text().replace("applicationId 'network.zoryq.budgetforecast'",f"applicationId 'network.zoryq.generated.{suffix}'")
          gradle.write_text(g)
          assert url in java.read_text()
          assert f'network.zoryq.generated.{suffix}' in gradle.read_text()
          assert name in manifest.read_text()

      - name: Build installable debug APK
        working-directory: apps/budget-dapp-android
        run: gradle :app:assembleDebug --no-daemon

      - name: Prepare evidence
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist
          cp apps/budget-dapp-android/app/build/outputs/apk/debug/app-debug.apk "dist/ZORYQ-${APP_SLUG}.apk"
          sha256sum dist/*.apk | tee dist/SHA256SUMS.txt
          printf '%s\n' "name=${APP_NAME}" "url=${APP_URL}" "source_commit=${GITHUB_SHA}" "claim=debug APK personalized to one generated app URL" > dist/BUILD-MANIFEST.txt

      - uses: actions/upload-artifact@v4
        with:
          name: ZORYQ-${{ env.APP_SLUG }}-APK-${{ github.run_number }}
          path: |
            dist/*.apk
            dist/SHA256SUMS.txt
            dist/BUILD-MANIFEST.txt
          if-no-files-found: error
          retention-days: 30
